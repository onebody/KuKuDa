# 2026-06-03 工作日志

## AI6800 配置项删除
- 删除前端 `ApiSettingsModal` 中的 AI6800 整个配置卡片
- 删除 `ApiSettingsState` / `defaultState` / `loadApiSettings` 中的 `ai6800` 字段
- 删除 `nodeExecutionService.ts` 中的 `callAi6800` 函数及所有 ai6800 引用
- 删除 `LLMNode.tsx` 中的 ai6800 默认模型和 JSX 分组
- 前端 TypeScript 编译通过（0 错误）

## 后端 AI 代理路由
- 新增 `/api/ai/chat` 和 `/api/ai/images` 代理路由（`backend/src/routes/aiRoutes.ts`）
- 前端 `nodeExecutionService.ts` 改为走后端代理，避免 CORS 和空 URL 问题
- 添加非 JSON 响应防护，给出明确错误提示

## 后端认证开发模式跳过
- `authMiddleware` 在 `NODE_ENV === 'development'` 时自动注入数据库真实用户 ID
- 解决前端未登录导致 401、画布空白的问题
