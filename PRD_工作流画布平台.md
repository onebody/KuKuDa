# 产品需求文档 (PRD)
## 多用户工作流式画布平台

**文档版本**: v1.0  
**创建日期**: 2026-06-01  
**产品经理**: 许清楚  
**审核状态**: 待审核

---

## 一、产品目标与核心价值

### 产品目标
打造一款类似 ComfyUI 和 Coze 的多用户工作流式画布管理平台，让用户通过可视化节点编辑器，以低代码/无代码方式构建和执AI 工作流。

### 核心价值
1. **降低AI使用门槛**：通过可视化节点编排，让非技术人员也能构建AI应用
2. **提高工作效率**：将复杂的AI调用流程标准化、模板化
3. **促进协作共享**：支持工作流模板分享，避免重复造轮子

---

## 二、用户故事

### 用户角色
- **普通用户**：创建和执行工作流
- **管理员**：管理用户、查看系统日志
- **开发者**：扩展节点类型、接入新AI模型

### 核心用户故事

**故事1：注册登录**
- 作为一个新用户，我想要注册账号并登录，以便使用工作流平台

**故事2：创建工作流**
- 作为已登录用户，我想要创建一个新的工作流，以便编排AI任务

**故事3：编辑画布**
- 作为工作流创建者，我想要在画布上拖拽节点、连接节点，以便构建工作流程

**故事4：执行工作流**
- 作为工作流创建者，我想要执行工作流并查看结果，以便完成AI任务

**故事5：管理我的工作流**
- 作为已登录用户，我想要保存、加载、重命名和删除我的工作流，以便管理我的创作

---

## 三、需求池

### P0（核心功能，必须实现）

| 需求ID | 需求描述 | 验收标准 |
|---------|----------|-----------|
| REQ-001 | 用户注册、登录、登出 | 能成功注册新账号、登录、登出 |
| REQ-002 | 工作流创建 | 点击"新建"按钮能创建工作流并进入编辑器 |
| REQ-003 | 节点库展示 | 左侧面板展示可用节点类型（文本输入、LLM、图片生成等） |
| REQ-004 | 节点拖拽到画布 | 从节点库拖拽节点到画布，节点显示正确 |
| REQ-005 | 节点连接 | 从一个节点的输出句柄拖拽到另一个节点的输入句柄，形成连接 |
| REQ-006 | 工作流保存 | 点击"保存"按钮，工作流数据保存到后端 |
| REQ-007 | 工作流加载 | 从工作流列表点击工作流，能加载到画布 |
| REQ-008 | 单节点执行 | 点击节点上的"执行"按钮，该节点调用AI模型并返回结果 |
| REQ-009 | 文本输入节点 | 支持输入文本，输出文本 |
| REQ-010 | LLM调用节点 | 支持选择模型（GPT、Claude等），输入提示词，输出文本结果 |

### P1（重要功能，优先实现）

| 需求ID | 需求描述 | 验收标准 |
|---------|----------|-----------|
| REQ-011 | 工作流列表展示 | 仪表盘展示用户创建的所有工作流 |
| REQ-012 | 工作流重命名 | 能修改工作流名称 |
| REQ-013 | 工作流删除 | 能删除工作流（需确认） |
| REQ-014 | 执行状态反馈 | 节点执行时显示"运行中"状态，执行完成显示"成功"或"失败" |
| REQ-015 | 画布缩放、平移 | 支持鼠标滚轮缩放、拖拽画布平移 |
| REQ-016 | 节点参数编辑 | 点击节点，右侧面板显示可编辑参数 |
| REQ-017 | 图片生成节点 | 输入提示词，调用图片生成API，输出图片 |
| REQ-018 | 图片展示 | 节点执行结果能展示生成的图片 |

### P2（增强功能，有时间实现）

| 需求ID | 需求描述 | 验收标准 |
|---------|----------|-----------|
| REQ-019 | 撤销/重做 | 支持 Ctrl+Z 撤销、Ctrl+Shift+Z 重做 |
| REQ-020 | 工作流模板 | 提供预置模板，一键创建 |
| REQ-021 | 区域选择 | 框选多个节点，批量移动或删除 |
| REQ-022 | 工作流分享 | 生成分享链接，他人可复制 |
| REQ-023 | 执行历史记录 | 查看工作流的历史执行记录和结果 |

---

## 四、UI设计建议

### 整体布局（三栏布局）

```
+-------------------------------------------------------+
|  顶部工具栏（保存、执行、撤销、重做、缩放）           |
+--------+------------+----------------+----------------+
| 左侧   |  中间      |  右侧         |  可选底部      |
| 节点库  |  画布      |  属性面板      |  执行日志      |
| (200px) |  (flex)   |  (300px)      |  (200px高)    |
|         |            |                |                |
+--------+------------+----------------+----------------+
```

### 关键界面

**1. 登录页**
- 邮箱 + 密码登录
- 注册入口
- 忘记密码（P2）

**2. 仪表盘（工作流列表）**
- 顶部：新建按钮、搜索框
- 卡片列表展示工作流（名称、创建时间、状态）
- 点击卡片进入编辑器

**3. 工作流编辑器**
- 顶部工具栏：保存、执行、撤销、重做、缩放控制
- 左侧节点库：节点分类（输入、处理、输出）、拖拽可用
- 中间画布：React Flow画布、节点、连接线
- 右侧属性面板：选中节点时显示可编辑参数
- 可选底部执行日志：实时显示执行状态

---

## 五、待确认问题

### 技术相关
1. **AI模型API密钥管理**：每个用户自己配置API密钥，还是平台统一配置？
   - 建议：P0阶段平台统一配置（后端.env），P1阶段支持用户自定义

2. **工作流执行方式**：单节点执行 vs 全工作流自动执行？
   - 建议：P0阶段只支持单节点执行，P1阶段支持全工作流执行

3. **实时通信方案**：Socket.IO vs SSE (Server-Sent Events)？
   - 建议：使用 Socket.IO（双向通信更灵活）

### 产品相关
1. **节点类型优先级**：第一批应该实现哪些节点？
   - 建议：文本输入、LLM调用、图片生成、文本输出、图片输出

2. **工作流是否支持版本管理**？
   - 建议：P2功能，初期不支持

---

## 六、非功能性需求

| 类型 | 需求 |
|------|------|
| 性能 | 画布支持 100+ 节点不卡顿 |
| 安全性 | 用户密码加密存储、JWT认证、API限流 |
| 可靠性 | 节点执行失败有错误提示、工作流自动保存 |
| 兼容性 | 主流浏览器（Chrome、Firefox、Safari） |

---

## 七、项目里程碑（建议）

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| P0核心功能 | 1-2周 | 用户认证、画布编辑器、单节点执行 |
| P1重要功能 | 1周 | 工作流管理、执行反馈、参数编辑 |
| P2增强功能 | 1周 | 撤销重做、模板、分享 |

---

**文档结束**
